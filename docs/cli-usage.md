# CLI Usage

GLITCHLAB provides a command-line interface for interacting with the engine.

## Identity
The CLI displays the official GLITCHLAB banner and version information stored in `glitchlab/identity.py`.

## Commands
All commands are routed through `glitchlab/cli.py`. Ensure that any new commands utilize the internal helper functions for consistent output formatting.