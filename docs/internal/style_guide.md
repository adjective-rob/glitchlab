

## Documentation Standards
All public APIs must use Google-style docstrings. This ensures clarity in parameter types, return values, and exception handling, particularly for complex logic such as cost estimation and tokenization.